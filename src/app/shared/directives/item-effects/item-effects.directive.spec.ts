import { ItemEffectsDirective } from './item-effects.directive';

describe('ItemEffectsDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemEffectsDirective();
    expect(directive).toBeTruthy();
  });
});
